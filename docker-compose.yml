services:
  miyo:
    image: miyo
    environment:
      HOST_ADDRESS: 192.168.1.11
    # ports:
    #   - "53:8080"
    volumes:
        - /var/run/docker.sock:/var/run/docker.sock
    network_mode: host
